{
  "name": "free-water-tips-db",
  "version": "1.0.0",
  "description": "Database management scripts for Free Water Tips",
  "main": "index.js",
  "scripts": {
    "db:clean": "node scripts/clean.js",
    "db:seed": "node scripts/seed.js",
    "db:reset": "npm run db:clean && npm run db:seed"
  },
  "dependencies": {
    "@azure/cosmos": "^4.0.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0"
  }
}
